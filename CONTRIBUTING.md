# Contributing

Use Node 22. Create focused branches, keep company claims traceable to approved source material, and never replace placeholders with guessed information. Before opening a PR run `npm run validate` and `npm run build`.
